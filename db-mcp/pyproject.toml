[project]
name = "db-mcp"
version = "0.1.0"
description = "PostgreSQL MCP Server"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastmcp",
    "asyncpg",
    "pyyaml",
    "python-dotenv",
    "uvicorn>=0.40.0",
    "aiohttp>=3.13.5",
]
